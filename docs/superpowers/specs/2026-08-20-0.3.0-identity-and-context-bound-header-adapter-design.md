# 0.3.0 Identity and ContextBound HeaderProtocolAdapter Design

**Status:** Approved on 2026-08-20; revised 2026-08-20 after review (collision-free encoding, seal/open mismatch errors, both API snapshots)

**Date:** 2026-08-20

**Target repository:** this repository (`gmcrypto-envelope-lite`)

**Target version:** 0.3.0

## 1. Purpose

Open 0.3.0 development on `main` without waiting for crates.io publication of 0.2.0, then close the documented gap that `HeaderProtocolAdapter` can only emit `AuthenticationContext::legacy()`.

Today a header-mapped integration that wants `AuthenticationMode::ContextBound` must write a custom `ProtocolAdapter`. This phase adds an exclusive schema acknowledgement on the existing adapter that emits crate-owned, collision-free context bytes, without changing `ProtocolAdapter`, SDK transcript framing, or envelope cryptography. Custom adapters remain free to supply their own context bytes; they are not required to match this encoding.

This specification does not change code, does not retarget `v0.2.0`, and does not publish to crates.io.

## 2. Current baseline

Facts verified at design time against annotated tag `v0.2.0` (`c7075b75d7460327d741814a15ff28fa1c1ec478`), which is also `main` at design time:

- Crate version is `0.2.0`. The GitHub repository is public. The crate is **not** on crates.io; live `cargo publish --locked` failed with 403 because the local token lacked publish permission. Publication is delayed by owner decision and, when it happens, must run from `git checkout v0.2.0`, not from a later `main`.
- `HeaderSchemaBuilder` requires `.legacy_authentication()` or `build()` returns `AdapterErrorKind::MissingField`. `HeaderProtocolAdapter::request_authentication_context` always returns `AuthenticationContext::legacy()`. `parse_response` always attaches `AuthenticationContext::legacy()` and does not read the request-id header from the response.
- `ProtocolAdapter` is object-safe, unsealed, and implemented outside the crate. Adding a required trait method is breaking (`docs/api-stability.md`).
- `AuthenticationMode::ContextBound` already exists. Transcript version 1 is `0x01 || u64be(domain_len) || domain || u64be(context_len) || context || u64be(plaintext_len) || plaintext`. The adapter supplies only the context bytes; the SDK frames them.
- `AuthenticationContext::context_bound` rejects empty bytes. `ProtocolRequestContext` rejects trimmed-empty operation strings and CR/LF. Request metadata likewise rejects trimmed-empty request ids.
- The README primary example and `examples/build_request.rs` construct ASCII `operation={op}&request-id={id}` context bytes. That recipe is **not** collision-free (distinct `(operation, request_id)` pairs can encode identically) and is **not** the encoding this adapter will emit. Custom adapters may keep it; `HeaderProtocolAdapter` uses the binary layout in §7.2.
- Outbound `authentication_input` / `aead_aad` kind mismatch returns `Error::AuthenticationContext`. Inbound CBC and AEAD open paths map that failure to `Error::InvalidEnvelope` (`src/envelope_crypto/cbc.rs` after `authentication_input`, `src/envelope_crypto/aead.rs` after `aead_aad` and `authentication_input`).
- `ci/check-public-api.sh` derives snapshot filenames from `Cargo.toml`. `CipherLocation` stays exhaustive. Fuzz `transport_parts` / `typed_headers` schemas use `.legacy_authentication()`.
- Versioned documents: security model, API-stability policy, engineering evidence, and cryptographic inventory are all **version 2**. This phase does not revise those policy versions.

## 3. Goals

- Move crate identity on `main` to 0.3.0 so 0.2.0 remains the immutable tagged line.
- Teach `HeaderProtocolAdapter` to produce and consume ContextBound authentication through an explicit schema acknowledgement, using one crate-owned encoding.
- Keep `.legacy_authentication()` as the required acknowledgement for plaintext-only header mappings.
- Leave `ProtocolAdapter`, `SecureClient`, `AuthenticationMode`, envelope crypto, and fuzz corpus bytes unchanged except where tests must observe the new acknowledgement.

## 4. Non-goals

- SM4-CCM, replay helpers, streaming, or server-side open-request / seal-response APIs.
- Charter §8 min-and-newest `gmcrypto-core` CI on every PR (still deferred).
- Live `cargo publish`, retargeting `v0.2.0`, or storing `CARGO_REGISTRY_TOKEN` in GitHub Actions.
- Configurable context field lists, format strings, or URL-encoding of field values.
- A second public adapter type (`BoundHeaderProtocolAdapter` or similar).
- Passing `ClientConfig` / `AuthenticationMode` into `HeaderProtocolAdapter`.
- Comparing the echoed response request-id to the outbound request-id inside `parse_response` or `SecureClient`. Request/response correlation remains an application check.
- New public error variants.
- A new fuzz target or ContextBound `transport_parts` schema in this phase.
- Bumping security-model, API-stability, evidence, or inventory **document** versions.

## 5. Sequencing

Two trains on `main`. Do not mix them in one commit.

### 5.1 Train A — 0.3.0 identity

No adapter or crypto behavior change. After this train, public-API snapshots are content-identical to 0.2.0; only filenames and crate version change.

`v0.2.0` stays at `c7075b75d7460327d741814a15ff28fa1c1ec478`. Later `main` must not be used as the 0.2.0 publish checkout.

### 5.2 Train B — ContextBound `HeaderProtocolAdapter`

Additive public API: `HeaderSchemaBuilder::context_bound_authentication`. Behavior change is confined to schemas that opt into that method. Existing `.legacy_authentication()` fixtures, fuzz seeds, and compatibility walkthroughs stay valid.

## 6. Train A — identity

### 6.1 Manifest, RC, and snapshots

- `Cargo.toml` `version = "0.3.0"`. Refresh `Cargo.lock` so only this crate's `version` line changes.
- `ci/check-cargo-package.sh`: expected package version `0.3.0`.
- `ci/check-release-candidate.sh`: `package_version=0.3.0`.
- `.github/workflows/release-candidate.yml` artifact name `gmcrypto-envelope-lite-0.3.0-rc-built-...`, and the matching literal in `tests/workflows.sh`.
- `tests/release_candidate.sh`: substitute crate-identity `0.2.0` fixture literals for `0.3.0`, reviewing each hit. Do **not** rewrite the static-guard fixture `/usr/local/bin/git -c user.name=fixture tag v0.2.0`; that string detects a `git tag` command, not this crate's version.
- `git mv` the two API snapshots to `api/gmcrypto-envelope-lite-0.3.0.txt` and `api/gmcrypto-envelope-lite-0.3.0-aead.txt`. Content stays identical until Train B. `ci/check-public-api.sh` and `tests/public_api.sh` already derive snapshot paths and drift messages from `Cargo.toml`; they must not be edited for the version string. Update `tests/release_documents.rs` markers that still quote `0.2.0` snapshot paths.

### 6.2 Documents

- `CHANGELOG.md`: add a Keep a Changelog `## [Unreleased]` section above `[0.2.0]`. Keep `[0.2.0] - 2026-08-19` historical. Train A records the identity move under `[Unreleased]`; Train B adds the adapter there under Added. Do not create a dated `[0.3.0]` heading until that version is actually released.
- `RELEASE_CHECKLIST.md` title becomes `# 0.3.0 Release Candidate External Gate Checklist`. Template version stays 2. The blank template is not a completed 0.2.0 record.
- `README.md` §Release status: `main` is 0.3.0, unreleased. 0.2.0 remains the tagged first crates.io *candidate*; `rc-built` evidence is that tag. Do not claim crates.io publication has occurred. When 0.2.0 is published, it is from `v0.2.0` after the checklist gates.
- `SECURITY.md`: keep `| 0.2.x | Yes |`; add `| 0.3.x | Unreleased (\`main\`) |`. Fixes land on `main` and ship in the next 0.3.x release. There is still no 0.1.x support line. A 0.2.x patch from the tag is a separate later decision, not this phase.
- `docs/api-stability.md`: `Within 0.3.x`; snapshot filenames `0.3.0`. **Policy version** stays 2. After Train A, note that **both** 0.3.0 snapshots (default features and `--features aead`) are content-identical to their 0.2.0 counterparts. Train B adds an always-enabled builder method, so **both** snapshots change and neither remains content-identical to 0.2.0.
- `SECURITY_MODEL.md`: keep **Model version:** 2. Change the “for `gmcrypto-envelope-lite` 0.2.x” sentence to 0.3.x so the in-tree model describes the crate on `main`. Claims are unchanged.
- `tests/release_documents.rs`: update markers that quote 0.2.0 identity (`Within 0.2.x`, snapshot paths, SECURITY table, the `publish = false` assertion message). Publication stays enabled; `publish = false` must remain absent.

## 7. Train B — adapter

### 7.1 Schema acknowledgement

`HeaderSchema` stores a private authentication kind: `Legacy` or `ContextBound`. It is not a public enum.

`HeaderSchemaBuilder` keeps two booleans (or equivalent flags):

- `.legacy_authentication()` sets the legacy flag. Calling it twice is last-wins (still `true`).
- `.context_bound_authentication()` sets the bound flag. Calling it twice is last-wins.

`build()`:

| Flags | Result |
| --- | --- |
| neither | `AdapterErrorKind::MissingField` (today’s missing-acknowledgement path) |
| both | `AdapterErrorKind::InvalidMapping` |
| only legacy | `HeaderSchema` kind `Legacy` |
| only context-bound | `HeaderSchema` kind `ContextBound` |

All existing required mappings remain required in both kinds. `Debug` for the builder reports both flags. `Debug` for `HeaderSchema` may show the kind; it must not print context bytes (there are none stored on the schema).

`legacy_authentication` rustdoc must stop saying the adapter cannot produce context-bound authentication. It selects plaintext-only signatures for this schema. `context_bound_authentication` rustdoc states: both peers reconstruct the encodings in §7.2 from typed fields; this is not AEAD, not replay protection, and not request/response correlation; `ClientConfig` must pin `AuthenticationMode::ContextBound` separately.

### 7.2 Pinned encodings

`HeaderProtocolAdapter` owns a versioned, direction-tagged, length-prefixed **binary** context. Peers reconstruct it from typed fields; they do not parse a concatenated string. Interior `&`, `=`, and whitespace are ordinary UTF-8 payload bytes inside a length-delimited field.

This encoding is **not** the ASCII `operation={op}&request-id={id}` recipe in the README custom-adapter example. That recipe is collision-prone and remains valid only for adapters that both ends agree on independently.

#### Version and direction

| Byte | Value | Meaning |
| --- | --- | --- |
| 0 | `0x01` | Header-adapter context version 1 |
| 1 | `0x01` | Request |
| 1 | `0x02` | Response |

Unknown versions and unknown direction tags are not decoded by this crate: the adapter only constructs bytes. A later version would be a coordinated wire change under a new acknowledgement or a new version byte, not silent fallback.

Lengths are unsigned 64-bit big-endian byte counts, the same width as `AuthenticationMode` transcript fields. Field bodies are the UTF-8 bytes of the typed string, with no terminator.

**Request** (`request_authentication_context`):

```text
0x01 || 0x01 || u64be(operation_len) || operation || u64be(request_id_len) || request_id
```

`operation` is `ProtocolRequestContext::operation()`. `request_id` is `context.metadata().request_id()`. Identity, request time, API version, and certificates are not included.

**Response** (`parse_response`, ContextBound schemas only):

```text
0x01 || 0x02 || u64be(request_id_len) || request_id
```

`request_id` is the value of the header named `HeaderSchema.request_id_header` on the **response**, matched case-insensitively like other response mappings.

Known-answer for the existing demo fixtures (`operation=demo-operation`, `request_id=demo-request-1`; both strings are 14 UTF-8 bytes):

- Request: `01 01 00 00 00 00 00 00 00 0e` ‖ `demo-operation` ‖ `00 00 00 00 00 00 00 0e` ‖ `demo-request-1`
- Response: `01 02 00 00 00 00 00 00 00 0e` ‖ `demo-request-1`

Wrap the constructed bytes with `AuthenticationContext::context_bound`. If that constructor fails, return `AdapterErrorKind::InvalidField`.

#### Surrounding whitespace: reject, do not canonicalize

A field used in this encoding is accepted only when `value == value.trim()`, using Unicode `str::trim()` (the same class as `validate_context_value` / `trimmed_nonempty`). Surrounding whitespace is `AdapterErrorKind::InvalidField`. Interior whitespace is kept and length-prefixed.

Rationale: `ProtocolRequestContext` currently stores untrimmed values whenever `trim()` is nonempty, and `build_request` emits those raw strings. Silently trimming before encoding would sign different bytes than the wire headers carry. Rejecting keeps encoded bytes identical to the typed strings that are emitted.

ContextBound response request-id therefore must **not** go through `trimmed_nonempty`. After `HeaderValue::new`, if the raw value has surrounding whitespace or is empty after trim, return `InvalidField`; otherwise encode the raw value. Other response fields (signature, wrapped key, cert, cipher) keep today’s trim behavior.

Legacy `parse_response` continues to ignore the request-id header.

Response errors for the ContextBound request-id slot:

- missing → `MissingField`
- surrounding whitespace or empty after trim → `InvalidField`
- case-insensitive duplicate → `DuplicateField`
- value rejected by `HeaderValue` → `InvalidField`

The echoed request-id is **not** compared to any outbound request-id. `parse_response` still does not receive the outbound `ProtocolRequestContext`.

### 7.3 Collision rule

For ContextBound schemas only, `validate_collisions` inserts `request_id_header` into the **response** name set. `HeaderName` equality is case-insensitive (canonical form), so `X-Demo-Request-Id` colliding with `x-demo-response-signature` is `InvalidMapping` the same as an exact-case clash. A ContextBound schema whose request-id header name equals `response_signature_header`, `response_wrapped_key_header`, `response_remote_signing_certificate_header`, or a header-placed `response_cipher` name fails `build()` with `InvalidMapping`.

Legacy schemas do not gain this extra response collision, because they do not read request-id on the response.

### 7.4 `ProtocolAdapter` and `SecureClient`

No trait methods added or given defaults. `HeaderProtocolAdapter` remains one type.

The adapter still does not see `ClientConfig`. Kind mismatch between `AuthenticationMode` and the adapter’s `AuthenticationContext` is unchanged envelope behavior, not a new adapter error:

- **Outbound** (`SecureClient::seal` / `build_request` → `authentication_input`, and AEAD `aead_aad` on seal): `Error::AuthenticationContext`.
- **Inbound** (`SecureClient::open` / `open_response`): the CBC and AEAD open paths map that failure to `Error::InvalidEnvelope` so inbound cryptographic failures stay one public category.

A ContextBound schema with a `LegacyPlaintext` client, and a legacy schema with a `ContextBound` client, therefore fail `build_request` with `AuthenticationContext` and fail `open_response` with `InvalidEnvelope`. There is no negotiation and no fallback.

`build_request` mapping is unchanged for both kinds: it still emits operation, request-id, and the other schema headers. ContextBound does not add headers; it only changes which `AuthenticationContext` is returned on seal and open.

### 7.5 Public API

Train B adds `pub fn HeaderSchemaBuilder::context_bound_authentication(self) -> Self`. The method is always present (not `aead`-gated). Regenerate **both** 0.3.0 snapshots. Each gains the new method; neither remains content-identical to its 0.2.0 counterpart.

`ProtocolAdapter`, `CipherLocation`, `AuthenticationContext`, and `AuthenticationMode` stay as they are.

## 8. Tests

### 8.1 Train A

Existing release-document, package, RC, workflow, and public-API contract tests must pass after identity substitution. No new behavioral tests.

### 8.2 Train B

Prefer extending `tests/protocol_adapter.rs` (and a `SecureClient` mismatch case) over new files.

Required coverage:

- `build()` neither acknowledgement → `MissingField`; both → `InvalidMapping`; either alone with a complete mapping → `Ok`.
- ContextBound `request_authentication_context` on the demo fixtures equals `AuthenticationContext::context_bound` of the request KAT in §7.2.
- Distinct `(operation, request_id)` pairs that collide under ASCII concatenation produce **different** request encodings. Required pair: `("pay", "1&request-id=2")` vs `("pay&request-id=1", "2")`.
- Surrounding whitespace on operation or request-id is `InvalidField` (request path). Interior whitespace is preserved and changes the length prefix.
- ContextBound `parse_response` with an echoed request-id header (including a case-differing header name) equals the response KAT in §7.2.
- ContextBound `parse_response` missing / surrounding-whitespace / duplicate request-id header → `MissingField` / `InvalidField` / `DuplicateField`.
- ContextBound schema whose `request_id_header` collides with a response mapping → `InvalidMapping`.
- Legacy `parse_response` still succeeds without a request-id header and still returns `AuthenticationContext::legacy()`.
- `SecureClient` with a ContextBound schema and `AuthenticationMode::LegacyPlaintext` (and the inverse): `build_request` / `seal` returns `Error::AuthenticationContext`; `open_response` / `open` of a counterpart-mode envelope returns `Error::InvalidEnvelope`. Pin both directions.
- Existing legacy protocol-adapter, support-schema, AEAD, redacted-debug, and fuzz fixtures that call `.legacy_authentication()` remain passing without encoding changes.

Do not switch `fuzz/fuzz_targets/support.rs` to ContextBound. Existing corpus bytes stay valid.

`complete_schema_builder()` remains legacy-only (still calls `.legacy_authentication()`). ContextBound tests start from `schema_builder_omitting(Some(RequiredMapping::LegacyAuthentication))` and then call `.context_bound_authentication()`, or an equivalent helper that never sets both flags.

The `RequiredMapping::LegacyAuthentication` omitting loop stays a missing-acknowledgement check: omitting that mapping still calls neither method and must remain `MissingField`. Do not add a second required mapping that every schema must set.

## 9. Documentation (Train B)

- README compatibility walkthrough keeps `.legacy_authentication()` and CBC + `LegacyPlaintext`.
- README stops saying `HeaderProtocolAdapter` cannot produce context-bound authentication. Describe `.context_bound_authentication()` plus a pinned `AuthenticationMode::ContextBound` as the header-mapped convenience when both ends use the §7.2 binary encoding. The primary “new integration” example may keep a custom `ProtocolAdapter`; its context bytes need not match §7.2.
- README migration **LegacyPlaintext → ContextBound**: header-mapped wires can use `HeaderProtocolAdapter` instead of a custom adapter when they adopt §7.2. It remains a coordinated both-ends wire change. Call out that ASCII concatenation is not this crate’s header-adapter encoding.
- `examples/build_request.rs` comment that the adapter “deliberately supports only legacy plaintext” is updated. The example may keep its custom adapter and ASCII context.
- Changelog `[Unreleased]` **Added**: exclusive `.context_bound_authentication()`, versioned direction-tagged length-prefixed encodings, extra response collision rule, surrounding-whitespace rejection.
- Engineering evidence map (still version 2): one row pointing at the new protocol-adapter tests for the pinned encodings and the XOR builder. No new security-model claims.

## 10. Implementation notes

- `AuthenticationContext::context_bound` returns `crate::Error`. Map it to `AdapterErrorKind::InvalidField` at the adapter boundary, matching `examples/build_request.rs` and `tests/secure_client.rs`.
- Response header scan: extend the existing `parse_response` loop rather than a second pass, so duplicate detection covers the request-id slot the same way as signature / wrapped-key / cert / cipher slots.
- Private helpers construct the binary layout (version, direction, `u64` big-endian lengths). Do not URL-encode. Do not emit ASCII `operation=` / `request-id=` labels inside these bytes.
- Do not reuse `trimmed_nonempty` for the ContextBound response request-id slot.

## 11. Out of scope reminders for later 0.3.x

These were considered and deferred, not forgotten:

- Peer/server helpers on `SecureClient`.
- SM4-CCM (`0x02` remains reserved).
- Charter min+max core CI on GitHub Actions.
- Publishing 0.2.0 or 0.3.0 to crates.io.
